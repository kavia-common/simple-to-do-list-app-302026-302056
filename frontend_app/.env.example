# Optional: override backend API base URL (defaults to http://localhost:3001)
# Preferred env var name:
REACT_APP_API_BASE=http://localhost:3001

# Backward compatible (also supported by the code):
# REACT_APP_API_BASE_URL=http://localhost:3001
